pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps { echo 'Checking out from SCM...' }
        }
        stage('Build') {
            steps { echo 'Building from Jenkinsfile...' }
        }
        stage('Test') {
            steps { echo 'Testing from Jenkinsfile...' }
        }
    }
}
